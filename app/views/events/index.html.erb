<br><%= link_to "Create Event", new_event_path %>

<% current_user.events.sort_by(&:date).each do |event| %>
  <ul>
    <li><%= event.formatted_date %> - <%= link_to event.name, event_path(event) %></li>
  </ul>
<% end %>
